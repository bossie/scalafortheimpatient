def pow(x: Double, n: Int): Double = {
	if (n > 0 && n % 2 == 0) {
		val y = pow(x, n / 2)
		y * y
	} else if (n > 0 && n % 2 != 0) {
		x * pow(x, n - 1)
	} else if (n == 0) {
		1
	} else {
		1 / pow(x, -n)
	}
}

