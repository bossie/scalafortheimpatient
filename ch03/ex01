scala> import scala.util.Random
import scala.util.Random

scala> def random(n: Int): Array[Int] =
     |   for (i <- (1 to n).toArray) yield Random.nextInt(n)
random: (n: Int)Array[Int]

scala> val a = random(5)
a: Array[Int] = Array(1, 3, 4, 1, 2)

